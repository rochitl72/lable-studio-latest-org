// Removed: live-collaboration WebSocket client.
//
// The single-user-per-project model replaced same-image multi-user co-editing,
// so the live-collaboration socket was removed. This module is intentionally
// empty (kept only so any stale import resolves). Safe to delete.
