# ─── Web API ─────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
pydantic==2.9.2
pydantic-settings==2.5.2

# ─── Database ────────────────────────────────────────────────
sqlalchemy==2.0.35
greenlet>=3.0.0
asyncpg==0.29.0          # PostgreSQL driver (production)
alembic==1.13.3
aiosqlite==0.20.0        # only used by the test suite, not at runtime

# ─── Image processing ────────────────────────────────────────
Pillow==10.4.0
numpy==1.26.4
opencv-python-headless==4.10.0.84

# ─── Auth / HTTP / files ─────────────────────────────────────
python-jose[cryptography]==3.3.0
# bcrypt is used directly rather than through passlib: passlib 1.7.4 reads
# bcrypt.__about__.__version__, removed in bcrypt 4.1, so the pair raises on
# first use. Talking to bcrypt directly avoids pinning it to an old release.
bcrypt>=4.0.0
httpx==0.27.2
aiofiles==24.1.0

# ─── Export formats ──────────────────────────────────────────
pycocotools>=2.0.8
PyYAML>=6.0.1
